server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo-database?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Loko1234@
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update    # En dev: auto-crea tablas
    show-sql: true



  jpa.defer-datasource-initialization: true  # data.sql después del schema
app:
  brand: "Mi Catálogo MVC"
